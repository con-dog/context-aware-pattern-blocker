// TODO: Load from storage patterns
export function loadRulesFromStorage() {
	//   chrome.storage.sync.get(['rawPatterns'], (result) => {
	//     if (result.rawPatterns) {
	//       textarea.value = result.rawPatterns.join('\n');
	//       updateCategoryView();
	//     }
	//   });
}

// TODO: Save rules to storage with debounce
